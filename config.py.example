"""
化工行业信息爬虫工具 - 配置文件示例
复制为 config.py 并填入你的真实配置
"""

import os
from pathlib import Path

BASE_DIR = Path(__file__).parent

DATABASE_PATH = BASE_DIR / "data" / "chem_info.db"

EMAIL_CONFIG = {
    "smtp_server": "smtp.163.com",
    "smtp_port": 465,
    "sender_email": "your_email@163.com",
    "sender_password": "YOUR_SMTP_AUTH_CODE",
    "recipient_email": "your_email@163.com",
    "use_ssl": True,
}

SPIDER_CONFIG = {
    "request_timeout": 30,
    "retry_times": 3,
    "retry_delay": 2,
    "user_agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
    "delay_between_requests": 1,
}

SCHEDULER_CONFIG = {
    "daily_push_time": "08:30",
    "timezone": "Asia/Shanghai",
}

FEISHU_CONFIG = {
    "enabled": False,
    "webhook_url": "YOUR_FEISHU_WEBHOOK_URL",
    "secret": "",
}

WECHAT_CONFIG = {
    "enabled": False,
    "push_type": "pushplus",
    "pushplus_token": "YOUR_PUSHPLUS_TOKEN",
    "serverchan_key": "YOUR_SERVERCHAN_KEY",
}

DEEPSEEK_API_KEY = "YOUR_DEEPSEEK_API_KEY"
DEEPSEEK_MODEL = "deepseek-chat"          # 主模型：日常分析
DEEPSEEK_VERIFY_MODEL = ""                # 可选复核模型，如 "deepseek-reasoner"；留空则不复核
PUSHPLUS_TEMPLATE = "markdown"            # 微信侧推送形态：markdown 文字报告（非图片）

# ---------------------------------------------------------------------------
# 涨跌分布主数据源（可选，强烈推荐）：复用本机 WorkBuddy 内置 westock-data 技能
# 腾讯自选股接口，独立于东方财富，从根上避免东方财富单源被限流时涨跌分布整块缺失。
# 将下方路径改为你本机 WorkBuddy 实际安装位置；留空("")则跳过该主源、退回东方财富兜底。
# ---------------------------------------------------------------------------
WESTOCK_CLI = r"D:\Program Files\WorkBuddy\resources\app.asar.unpacked\resources\builtin-skills\westock-data\scripts\index.js"
NODE_BIN = r"C:\Users\cheng\.workbuddy\binaries\node\versions\22.22.2\node.exe"

# ---------------------------------------------------------------------------
# 国内 A 股要闻加成层（可选）：复用本机 neodata-financial-search 技能
# token 由 WorkBuddy IDE 会话签发（connect_cloud_service），有有效期（约24h）。
# 留空("")则跳过该加成层，bot 推送不含国内 A 股要闻，主流程不受影响。
# 刷新方式：跟助手说"刷新 neodata token"，助手取新 token 填入 NEODATA_TOKEN。
# ---------------------------------------------------------------------------
NEODATA_TOKEN = ""   # tk_xxx 或 Bearer；由助手填入，临时有效
NEODATA_SKILL_DIR = r"D:\Program Files\WorkBuddy\resources\app.asar.unpacked\resources\builtin-skills\neodata-financial-search\scripts"

# ===== 国际财经 RSS（甲方案，零注册零 key）=====
# 默认开启；代码内置 BBC/CNBC/MarketWatch/AP/Economist 等公开源。
# 想关掉或自定义源，把下面这段复制到 config.py 并修改即可（代码已对此段做兜底，不加也能跑）。
WORLD_NEWS_CONFIG = {
    "enabled": True,          # 是否抓取国际财经要闻
    "max_items": 10,          # 每日并入报告的条数上限
    "sources": [],            # 留空则用内置默认源；或填 [{"name": "...", "url": "..."}, ...]
}

DATA_SOURCES = {
    "chemnet": {
        "name": "中国化工网",
        "base_url": "https://www.chemnet.com.cn",
        "enabled": True,
        "categories": {
            "news": "https://www.chemnet.com.cn/news/",
            "price": "https://www.chemnet.com.cn/price/",
        }
    },
    "oilchem": {
        "name": "隆众资讯",
        "base_url": "https://www.oilchem.net",
        "enabled": True,
        "categories": {
            "news": "https://www.oilchem.net/news/",
            "price": "https://www.oilchem.net/price/",
        }
    },
    "cnfeol": {
        "name": "中国铁合金在线",
        "base_url": "https://www.cnfeol.com",
        "enabled": True,
        "categories": {
            "news": "https://www.cnfeol.com/news/",
            "price": "https://www.cnfeol.com/price/",
        }
    },
}

CATEGORY_KEYWORDS = {
    "行情价格": ["价格", "行情", "报价", "走势", "涨跌", "上调", "下调", "成本"],
    "行业动态": ["投产", "扩建", "检修", "产能", "产量", "开工率", "新项目"],
    "市场分析": ["分析", "预测", "展望", "供需", "库存", "进出口", "贸易"],
    "政策法规": ["政策", "法规", "标准", "环保", "安全", "监管"],
}

CHEM_PRODUCTS = [
    "甲醇", "乙醇", "丙烯", "乙烯", "苯", "甲苯", "二甲苯",
    "PTA", "PVC", "PE", "PP", "PS", "ABS", "环氧树脂", "聚酯",
    "尿素", "纯碱", "烧碱", "硫酸", "硝酸", "盐酸",
    "MDI", "TDI", "聚醚", "多元醇", "有机硅",
]

WEB_CONFIG = {
    "host": "0.0.0.0",
    "port": 5000,
    "debug": False,
    "secret_key": os.urandom(24).hex(),
}

LOG_CONFIG = {
    "level": "INFO",
    "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    "file": BASE_DIR / "logs" / "app.log",
    "max_bytes": 10485760,
    "backup_count": 5,
}


def ensure_directories():
    dirs = [
        BASE_DIR / "data",
        BASE_DIR / "logs",
        BASE_DIR / "templates",
        BASE_DIR / "static",
        BASE_DIR / "static" / "css",
        BASE_DIR / "static" / "js",
    ]
    for d in dirs:
        d.mkdir(parents=True, exist_ok=True)


ensure_directories()


def validate_config():
    issues = []
    if EMAIL_CONFIG["sender_email"] == "your_email@163.com":
        issues.append("EMAIL: 发件人邮箱未配置")
    if EMAIL_CONFIG["sender_password"] == "YOUR_SMTP_AUTH_CODE":
        issues.append("EMAIL: SMTP授权码未配置")
    if not issues:
        issues.append("配置检查通过")
    return issues
